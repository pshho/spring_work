package figure.adv;

import org.aspectj.lang.JoinPoint;
import org.springframework.stereotype.Component;

@Component
public class FigureAdvice {
	void calcRectTri(JoinPoint joinPoint) {
		Object rev = null;
		System.out.println(joinPoint.getSignature().getName());
		
		String ptn1 = ".*Area";
		String ptn2 = ".*Perimeter";
		
		int sumArea = 0, sumPerimeter = 0;
		double avgArea = 0, avgPerimeter = 0;
		try {
			if(joinPoint.getSignature().getName().startsWith("rect")) {
				System.out.println(joinPoint.getArgs());
			}
		} catch (Throwable e) {
			e.printStackTrace();
		}
	}
}
